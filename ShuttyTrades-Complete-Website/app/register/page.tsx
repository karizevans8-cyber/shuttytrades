"use client";
import Link from "next/link"; import { useState } from "react";
export default function Register(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");const go=()=>{if(!name||!email||password.length<6){setMsg("Enter your name, a valid email and a password of at least 6 characters.");return}localStorage.setItem("st_user",JSON.stringify({name,email,balance:10000,equity:10000,kyc:"Not started",role:"USER"}));location.href="/dashboard"};return <main className="auth-page"><div className="auth-card"><Link href="/" className="brand"><span className="brand-mark">↗</span>Shutty<span>Trades</span></Link><h1>Create demo account</h1><p className="subtle">Start with a virtual $10,000 balance.</p><div className="field"><label>FULL NAME</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></div><div className="field"><label>EMAIL</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div><div className="field"><label>PASSWORD</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 6 characters"/></div>{msg&&<div className="wallet-warning" style={{marginTop:12}}>{msg}</div>}<button className="btn btn-primary" style={{width:"100%",marginTop:18}} onClick={go}>Create demo account</button><p className="auth-footer">Already registered? <Link href="/login">Sign in</Link></p><p className="auth-footer"><Link href="/">← Back to website</Link></p></div></main>}
