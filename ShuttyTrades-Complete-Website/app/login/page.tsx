"use client";
import Link from "next/link"; import { useState } from "react";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [err,setErr]=useState("");const go=()=>{if(!email||!password){setErr("Enter your email and password.");return}localStorage.setItem("st_user",JSON.stringify({name:email.split("@")[0]||"Demo Trader",email,balance:10000,equity:10000,kyc:"Not started",role:"USER"}));location.href="/dashboard"};return <main className="auth-page"><div className="auth-card"><Link href="/" className="brand"><span className="brand-mark">↗</span>Shutty<span>Trades</span></Link><h1>Welcome back</h1><p className="subtle">Sign in to your demo account.</p><div className="field"><label>EMAIL</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div><div className="field"><label>PASSWORD</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></div>{err&&<div className="wallet-warning" style={{marginTop:12}}>{err}</div>}<button className="btn btn-primary" style={{width:"100%",marginTop:18}} onClick={go}>Sign in</button><p className="auth-footer">Demo login only. <Link href="/register">Create an account</Link></p><p className="auth-footer"><Link href="/">← Back to website</Link></p></div></main>}
